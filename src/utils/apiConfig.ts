// Vercel provides a relative path for serverless functions, so we don't need a full URL.
// The '/api' path is automatically handled by Vercel's routing for serverless functions.
export const API_BASE_URL = '/api';